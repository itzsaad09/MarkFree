{
  "version": 2,
  "builds": [
    {
      "src": "api/remove_watermark.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "50mb" }
    }
  ],
  "routes": [
    { "src": "/remove_watermark", "dest": "api/remove_watermark.py" }
  ]
}